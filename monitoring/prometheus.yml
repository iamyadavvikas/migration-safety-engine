global:
  scrape_interval: 5s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: migration-safety-engine
    static_configs:
      # host.docker.internal lets Prometheus (in Docker) scrape the engine running on the host.
      - targets: ["host.docker.internal:8080"]
